import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Colors
  static const Color primary = Color(0xFF003DA5); // Royal Blue
  static const Color primaryDark = Color(0xFF002D7A); // Darker Royal Blue

  // Accent Colors
  static const Color accent = Color(0xFFFFC107); // Gold
  static const Color accentLight = Color(0xFFFFD54F); // Soft Gold

  // Neutral Colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFF8FAFC); // Slate Background
  static const Color textDark = Color(0xFF0F172A); // Dark Text
  static const Color textGrey = Color(0xFF64748B); // Slate Grey
  static const Color border = Color(0xFFE2E8F0); // Border Color

  // Semantic Colors
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);
}
